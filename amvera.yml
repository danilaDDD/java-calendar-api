meta:
  environment: jvm
  toolchain:
    name: gradle
    version: "11"
build:
  artifacts:
    build/libs/dannk-api-calendar.jar: /
  args: --stacktrace
run:
  jarName: dannk-api-calendar.jar
  persistenceMount: /data
  containerPort: "80"
